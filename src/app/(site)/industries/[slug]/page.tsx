import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowRight,CheckCircle2 } from "lucide-react";
import { getIndustryBySlug } from "@/lib/data";
import { BenefitsList,CTASection,PageHero,ProductCard,SectionHeading } from "@/components/site/ui";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const item=await getIndustryBySlug((await params).slug);return item?{title:item.metaTitle||`${item.name} Storage Solutions`,description:item.metaDescription||item.shortDescription}:{}}
export default async function IndustryPage({params}:{params:Promise<{slug:string}>}){const item=await getIndustryBySlug((await params).slug);if(!item)notFound();return <main><PageHero eyebrow="Industry solutions" title={`Storage systems for ${item.name.toLowerCase()}.`} description={item.shortDescription} image={item.heroImage} breadcrumb={[{label:"Industries",href:"/industries"},{label:item.name}]}/><section className="py-24"><div className="container-shell grid gap-14 lg:grid-cols-[.7fr_1.3fr]"><SectionHeading eyebrow="Operating context" title="Understand the storage challenge first."/><p className="text-xl leading-9 text-zinc-700">{item.description}</p></div></section><section className="surface-grid bg-[#f4f4f1] py-24"><div className="container-shell grid gap-12 lg:grid-cols-2"><div><SectionHeading eyebrow="Common challenges" title="Capacity and access must work together."/><ul className="mt-8 space-y-3">{(item.challenges||[]).map(c=><li key={c} className="flex gap-3 bg-white p-4 text-sm"><CheckCircle2 size={17} className="text-red-600"/>{c}</li>)}</ul></div><div><SectionHeading eyebrow="Potential benefits" title="A more deliberate storage response."/><div className="mt-8"><BenefitsList items={item.benefits||[]}/></div></div></div></section><section className="py-24"><div className="container-shell"><SectionHeading eyebrow="Recommended systems" title={`Storage options relevant to ${item.name.toLowerCase()}.`} description="The final selection depends on your inventory, unit loads, handling equipment and facility."/><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-4">{item.products.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div></div></section><section className="bg-zinc-950 py-24 text-white"><div className="container-shell"><SectionHeading eyebrow="Services" title="Support the decision before implementation." light/><div className="mt-10 grid gap-px bg-white/10 md:grid-cols-3">{item.services.map(s=><Link href={`/services/${s.slug}`} key={s.id} className="group bg-zinc-950 p-7 hover:bg-zinc-900"><h3 className="text-xl font-semibold">{s.name}</h3><p className="mt-3 text-sm leading-6 text-zinc-400">{s.shortDescription}</p><ArrowRight className="mt-7 text-red-500 transition-transform group-hover:translate-x-1" size={17}/></Link>)}</div></div></section><CTASection title={`Discuss your ${item.name.toLowerCase()} storage requirement.`}/></main>}
