import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { getIndustries } from "@/lib/data";
import { PageHero,SectionHeading,CTASection } from "@/components/site/ui";
import { SmartImage } from "@/components/site/smart-image";
export const metadata:Metadata={title:"Industries",description:"Storage systems and warehouse layouts configured for manufacturing, logistics, retail, e-commerce, offices and other operating environments."};
export default async function IndustriesPage(){const items=await getIndustries();return <main><PageHero eyebrow="Industries" title="Different operations demand different storage priorities." description="Inventory profile, handling method, access frequency and facility constraints shape every recommendation." image={items[0]?.heroImage} breadcrumb={[{label:"Industries"}]}/><section className="py-24"><div className="container-shell"><SectionHeading eyebrow="Operating environments" title="Find your industry context."/><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{items.map((item,i)=><Link href={`/industries/${item.slug}`} key={item.id} className="card-lift group relative min-h-[390px] overflow-hidden bg-zinc-950 text-white"><SmartImage src={item.heroImage||""} alt={`${item.name} storage`} fill className="object-cover opacity-50 transition duration-700 group-hover:scale-105" sizes="33vw"/><div className="absolute inset-0 bg-gradient-to-t from-black via-black/20 to-transparent"/><div className="absolute inset-x-0 bottom-0 p-7"><span className="text-xs font-bold text-red-400">{String(i+1).padStart(2,"0")}</span><h2 className="mt-3 text-2xl font-semibold">{item.name}</h2><p className="mt-3 text-sm leading-6 text-zinc-300">{item.shortDescription}</p><ArrowRight size={17} className="mt-6 transition-transform group-hover:translate-x-1"/></div></Link>)}</div></div></section><CTASection title="Plan storage around how your operation actually moves."/></main>}
