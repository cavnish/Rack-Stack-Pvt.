import type { Metadata } from "next";
import { PageHero,SectionHeading,ServiceCard,CTASection } from "@/components/site/ui";
import { Stagger,StaggerItem } from "@/components/site/reveal";
import { getServices } from "@/lib/data";
export const metadata:Metadata={title:"Storage Planning & Installation Services",description:"Storage planning, site survey, rack design, warehouse layout, installation and optimization services."};
export default async function ServicesPage(){const services=await getServices();return <main><PageHero eyebrow="Services" title="The thinking around the system matters." description="From site information and layout planning to installation coordination, our services help define a more relevant storage response." image={services[0]?.heroImage} breadcrumb={[{label:"Services"}]}/><section className="py-24"><div className="container-shell grid gap-14 lg:grid-cols-[.8fr_1.2fr]"><SectionHeading eyebrow="How we support projects" title="Practical expertise across the storage project lifecycle." description="Engage individual services or coordinate them with a storage system proposal."/><Stagger>{services.map((service,i)=><StaggerItem key={service.id}><ServiceCard service={service} index={i}/></StaggerItem>)}</Stagger></div></section><CTASection title="Bring us your storage challenge—not just a rack list."/></main>}
