"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
export function CookieConsent(){const [open,setOpen]=useState(false);const [manage,setManage]=useState(false);useEffect(()=>{const timer=window.setTimeout(()=>setOpen(!localStorage.getItem("rackstack_cookie_consent")),0);return()=>window.clearTimeout(timer)},[]);function choose(value:string){localStorage.setItem("rackstack_cookie_consent",value);setOpen(false)}if(!open)return null;return <aside className="fixed bottom-4 left-1/2 z-[80] w-[min(94vw,680px)] -translate-x-1/2 border border-zinc-700 bg-zinc-950 p-5 text-white shadow-2xl" aria-label="Cookie preferences"><div className="flex flex-col gap-4 md:flex-row md:items-center"><div className="grow"><p className="text-sm font-semibold">Your Privacy Preferences</p><p className="mt-1 text-xs leading-relaxed text-zinc-400">We use essential cookies to run the site. Optional analytics are only used if you allow them. <Link className="underline" href="/cookie-policy">Learn more</Link>.</p>{manage&&<label className="mt-3 flex items-center gap-2 text-xs"><input type="checkbox" disabled checked/> Essential cookies (required)</label>}</div><div className="flex shrink-0 flex-wrap gap-2"><button className="border border-zinc-600 px-3 py-2 text-xs" onClick={()=>setManage(!manage)}>Manage</button><button className="border border-zinc-600 px-3 py-2 text-xs" onClick={()=>choose("essential")}>Reject Optional</button><button className="bg-[#d11f2f] px-3 py-2 text-xs font-bold" onClick={()=>choose("all")}>Accept</button></div></div></aside>}
