"use client";
import { useState } from "react";
import { ArrowRight } from "lucide-react";
export function NewsletterForm(){const [email,setEmail]=useState("");const [state,setState]=useState<"idle"|"loading"|"done"|"error">("idle");async function submit(e:React.FormEvent){e.preventDefault();setState("loading");const response=await fetch("/api/newsletter",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setState(response.ok?"done":"error");if(response.ok)setEmail("")}return <form onSubmit={submit} className="mt-5"><label htmlFor="footer-email" className="sr-only">Business email</label><div className="flex border-b border-white/25"><input id="footer-email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Business email" className="min-w-0 grow bg-transparent py-3 text-sm outline-none placeholder:text-zinc-600"/><button aria-label="Subscribe" disabled={state==="loading"} className="p-3 text-red-400"><ArrowRight size={18}/></button></div>{state==="done"&&<p className="mt-2 text-xs text-green-400">You&apos;re subscribed.</p>}{state==="error"&&<p className="mt-2 text-xs text-red-400">Please try again.</p>}</form>}
